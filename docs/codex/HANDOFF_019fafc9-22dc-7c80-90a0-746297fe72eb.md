# 会话 019fafc9-22dc-7c80-90a0-746297fe72eb 续接 handoff

本文用于在新窗口或 compact 后恢复任务。它保留的是会话状态摘要，不是逐字 transcript，
也不授予任何新修改权限。恢复后仍须读取两级 `AGENTS.md`、适用 skills、
`PROJECT_CONTEXT.md`、`CURRENT_STATE.md`，并动态核对 working tree。

## 原始会话请求与续接任务

原始请求的核心是：在 mini-sglang 的 `System-test` 分支开发 GPT-OSS 原生推理原型，
覆盖模型结构、OpenAI Harmony chat、packed MXFP4 MoE、attention sinks/sliding window、
权重加载、CUDA graph 与服务接口，同时保持既有 Context System 的 Drop/Radix/position
不变量；最终只能通过本地 commit/push 后到 `InfiniAI-BUS` 验证。

后续续接任务要求接管已经 dirty 的共享 working tree，不丢失 GPT-OSS 与 Context System
既有改动，完成验证、修复、checkpoint 和远端测试。旧会话曾讨论 pasted-text/`symbol`
Radix 方案；当前 HEAD 后的真实默认已经是 token-position `delta-marker`，不能沿用旧默认。

## 计划历史

- `PLAN-CS-20260804-R1`：第一版只读审计与实现规划；未作为最终实施边界。
- `PLAN-CS-20260804-R2`：根据审计收紧兼容性、测试和远端验证要求；继续修订。
- `PLAN-CS-20260804-R3`：用户已明确批准的续接实现计划。当前 17 个 tracked dirty 文件、
  GPT-OSS untracked 实现和 tests 属于该计划的未 checkpoint 原型。

不要因本 handoff 或 compact 摘要自动继续写 R3 范围；新一轮 writer 仍须拿到明确的
`APPROVED_PLAN_ID`、`APPROVED_ROUND` 与 `ALLOWED_FILES`。

## R3 目标与当前原型覆盖

R3 目标是把 GPT-OSS 的关键推理链接入 mini-sglang，并与现有 Context System 共存：

- 注册 `GptOssForCausalLM`，解析 GPT-OSS 配置、alternating full/sliding attention、
  attention bias/sinks 与 MXFP4 属性；
- 映射和 TP shard 官方 packed MXFP4 权重，提供 Triton 3.6 ABI 的 routing、swizzle、
  SwiGLU 与两段 expert matmul；
- 使用 OpenAI Harmony 构造 system/developer/user/assistant/tool token stream，传递
  `reasoning_effort`，识别多 stop token 并解析 output channel/tool recipient；
- 为 attention backend 传递 sinks/window，并保持普通模型旧调用不扩参；
- 将 CUDA graph 改为 scheduler idle 时逐 batch-size capture，单 shape 失败时 eager fallback，
  新请求可打断 capture；
- 添加 config、weight、MXFP4、attention、CUDA graph 与最小 e2e focused tests；
- 保持默认 delta-marker/staged 的 full/active KV、absolute position、marker/page 生命周期。

当前 working tree 已出现上述原型代码和 tests，但“代码存在”不等于“功能已支持”。尤其
GPT-OSS 20B/120B、TP8、真实模型加载、Harmony 流式输出、MXFP4 kernel、decode/CUDA graph
和 Drop 共存都还缺少完整运行证据。

## 已做检查与未完成验证

续接记录只确认过 Python `compileall` 和 diff/scope 检查；没有 focused `pytest` 结果，
没有 commit/push，也没有在 `InfiniAI-BUS` 运行 `rosetta` 环境测试。当前验证状态必须视为
`NOT YET PASSED`。

下一步至少需要：

1. 动态复核 branch/HEAD/status/diff，确认没有覆盖其他参与者修改。
2. 运行新增 GPT-OSS focused tests 与既有 Context System CPU regression tests，按失败证据在
   获批范围内修复。
3. 复核 dependency/version 与 Python 3.10 兼容性，以及无需 GPU 的 import/collection。
4. 创建精确 checkpoint、push `origin/System-test`。
5. 在 `InfiniAI-BUS:/share/wangruoxi/repo/mini-sglang` fast-forward pull，激活 conda
   `rosetta`，运行真实 CUDA/TP/MXFP4/model-load/serve tests，并核对三端 HEAD。
6. 对 GPT-OSS + Drop 的 staged warmup、Radix reuse、abort/evict/cleanup 和无 Drop baseline
   做端到端回归。

## FA3 mask 的后续说明义务

后续报告必须明确区分两件事：FA3 路径可以承载普通 causal/sliding-window 与 sinks 参数，
但当前 Context System 的单次 full-context custom mask 实现依赖 FA4 `mask_mod`（或独立的
FlashInfer packed mask）。当前没有 FA3 Context System custom-mask 实现或验证；不能写成
“FA3 mask 已支持”。默认 `staged` 不依赖该 custom mask，这也不能被用来暗示 FA3 mask
能力。若未来要支持，必须重新审计 FA3 接口、单独规划、实现 focused/GPU tests 并更新
文档。

## 固定工作流与保护项

Codex 项目根保持 `/Users/x.puppet/Desktop/Undergraduate/Project/Contextual system`，本地仓库
是唯一编辑源。`results/`、所有既有 dirty GPT-OSS/Context System 代码和 tests 都不得清理。
远端主机为 `InfiniAI-BUS`，仓库 `/share/wangruoxi/repo/mini-sglang`，conda 环境
`rosetta`；严格执行 local edit -> commit/push -> remote `git pull --ff-only` -> test。
